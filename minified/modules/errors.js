function checkForExistingMessage(e,r){let t=[];return e.childNodes.forEach(e=>t.push(e.textContent)),t.includes(r)}const errorContainer=document.querySelector(".error-message");export function displayError(e){let r=document.createElement("p");r.textContent=e,errorContainer.appendChild(r)}export function clearErrors(){errorContainer.querySelectorAll("p").forEach(e=>e.remove())}export function validateTaskNameInput(e){const r="Task name too long: must be 200 characters or less. Extra characters will be removed.";e.checkValidity()?e.classList.remove("input-error"):(e.classList.add("input-error"),checkForExistingMessage(errorContainer,r)||displayError(r))}export function validateDurationInput(e){const r="Hours and minutes must be a whole number from 0 - 360. Numbers outside this range will be rounded to the closet valid number.";e.checkValidity()?e.classList.remove("input-error"):(e.classList.add("input-error"),checkForExistingMessage(errorContainer,r)||displayError(r))}